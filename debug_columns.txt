Users Table Columns:
id (int)
name (varchar(100))
email (varchar(100))
mobile (varchar(20))
password (varchar(255))
address (text)
city (varchar(100))
state (varchar(100))
pincode (varchar(20))
role (enum('customer','guest'))
created_at (timestamp)
updated_at (timestamp)
