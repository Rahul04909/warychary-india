Table: products
  id (int)
  name (varchar(255))
  slug (varchar(255))
  featured_image (varchar(255))
  gallery_images (text)
  short_description (text)
  description (longtext)
  mrp (decimal(10,2))
  purchase_price (decimal(10,2))
  sales_price (decimal(10,2))
  delivery_cost (decimal(10,2))
  packing_cost (decimal(10,2))
  total_cost (decimal(10,2))
  is_free_product_active (tinyint(1))
  free_product_name (varchar(255))
  free_product_image (varchar(255))
  status (enum('active','inactive'))
  created_at (timestamp)
  updated_at (timestamp)

Table: razorpay_settings
  id (int)
  key_id (varchar(255))
  key_secret (varchar(255))
  webhook_secret (varchar(255))
  currency (varchar(10))
  mode (enum('test','live'))
  created_at (timestamp)
  updated_at (timestamp)

Table: razorpay_transactions
  id (int)
  order_id (varchar(255))
  payment_id (varchar(255))
  amount (decimal(10,2))
  status (varchar(50))
  email (varchar(255))
  contact (varchar(50))
  created_at (timestamp)

Table: reviews
  id (int)
  product_id (int)
  user_name (varchar(100))
  user_email (varchar(100))
  rating (int)
  review_text (text)
  review_images (text)
  status (enum('pending','approved','rejected'))
  created_at (timestamp)

Table: senior_partners
  id (int)
  name (varchar(100))
  email (varchar(100))
  image (varchar(255))
  gender (enum('Male','Female','Other'))
  state (varchar(100))
  city (varchar(100))
  pincode (varchar(20))
  address (text)
  password (varchar(255))
  referral_code (varchar(6))
  commission (decimal(5,2))
  status (enum('active','inactive'))
  created_at (timestamp)
  updated_at (timestamp)

Table: smtp_settings
  id (int)
  host (varchar(255))
  username (varchar(255))
  password (varchar(255))
  port (int)
  encryption (enum('tls','ssl','none'))
  from_email (varchar(255))
  from_name (varchar(255))
  updated_at (timestamp)

